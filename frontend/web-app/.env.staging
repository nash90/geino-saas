# Staging Environment
# This file is for building the frontend for staging deployment
# Deployed to: geinosaas.yamacity.com

# Backend API URL (Cloudflare Worker - staging)
# This should point to your staging worker domain
VITE_API_URL=https://api-geinosaas.yamacity.com

# Note: NODE_ENV is automatically set by Vite build command
# Don't set NODE_ENV here - it will cause an error
